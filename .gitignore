# Product artifacts — not part of the doctor tool
observo_installer

# Site-specific values: contains live credentials (siteSpacePassword). Do NOT commit.
site.yaml

# Backups / editor / OS cruft
*.bak
*.swp
.DS_Store
INCIDENT-REPORT.md
customer-network-guide.md
network-requirements.md

# --- Local development artifacts (fork-only; never push upstream) ---
# AI assistant + planning scratch files
CLAUDE.md
PLAN.md

# Docker Desktop / local Docker config & overrides
.docker/
docker-compose.override.yml
docker-compose.*.local.yml
*.local.yml
